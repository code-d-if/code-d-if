# codedif.com

Static marketing site for Code D'If.

## Local preview

```bash
python3 -m http.server 8080
```

Then open <http://localhost:8080>.

## GitHub Pages

The site is plain HTML/CSS and can be served from the repository root on the `main` branch. The `CNAME` file points GitHub Pages at `codedif.com`.
